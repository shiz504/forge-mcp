[project]
name = "forge-mcp"
version = "0.1.0"
description = "OAuth-protected MCP wrapper for forge-ingest"
requires-python = ">=3.12,<3.13"
dependencies = [
    "fastmcp==3.3.1",
    "httpx==0.28.1",
    "uvicorn[standard]==0.47.0",
]

[tool.setuptools.packages.find]
include = ["app*"]
